#ifndef VTKOCC_IMPORT_CPP_H
#define VTKOCC_IMPORT_CPP_H

#include <iostream>
#include <sstream>
#include <fstream>
#include <string>
#include <regex>
#include <vector>
#include <map>
#include <queue>
#include <deque>
#include <tuple>
#include <chrono>
#include <exception>
#include <algorithm>
#include <functional>
#define TO_STR(S) #S
#define LOG(STR) std::cout << STR << std::endl
#define R_PTR(P) if (!P) {delete P; P = nullptr;}

#endif //VTKOCC_IMPORT_CPP_H
